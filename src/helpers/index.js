export function booleanFormater(value, key = null, item = null) {
  return value ? 'Yes' : 'No'
}

// export  function formatDecimal (value, no=BusinessSetting.decimal_up_to) { return Number(value).toFixed(no) }
